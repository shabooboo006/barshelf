# Internal project docs — NOT published to the public repo
CLAUDE.md
AGENTS.md
docs/

# macOS
.DS_Store

# Xcode / Swift build artifacts
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/

# Release artifacts — published to GitHub Releases, never committed
*.dmg
*.pkg
*.zip

# Secrets & Apple signing material — NEVER commit (rotate immediately if leaked)
.env
.env.*
*.p8
*.p12
*.cer
*.pem
*.certSigningRequest
*.mobileprovision
*.keychain
*.keychain-db
AuthKey_*.p8
id_rsa*
secrets/
